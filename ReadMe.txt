A simple animated hamburger menu made with html/css/bootstrap and jquery. It has a hamburger collapsible menu and a sign in and login button.
If the flexbox on the header element does not work, set the child containers(left-side+right-side) to float.
